# Copyright (C) 2013-2017 Nicolas Lamirault <nicolas.lamirault@gmail.com>

# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

stages:
  - lint
  - configure
  - test

image: docker:stable

variables:
  DOCKER_DRIVER: overlay2
  ANSIBLE_VERSION: 2.4.0.0
  PIP_CACHE_DIR: "$CI_PROJECT_DIR/.pip"

services:
  - docker:dind

cache:
  paths:
    - .pip/
    - virtenv/

before_script:
  - docker info
  - apk add --update python py-pip python-dev
  - apk add --update curl && rm -rf /var/cache/apk/*
  - pip install virtualenv

.gitlab_molecule: &gitlab_molecule
  before_script:
    # - apt-get install python3-pip python3-virtualenv
    # - pip3 install virtualenv
    # - docker info
    - virtualenv virtenv
    - source virtenv/bin/activate
    - pip3 install ansible molecule docker


# ================================
# lint

lint:debug:
  <<: *gitlab_molecule
  stage: lint
  script:
    - cd debug && molecule lint

lint:setup:
  <<: *gitlab_molecule
  stage: lint
  script:
    - cd debug && molecule lint

lint:base:
  <<: *gitlab_molecule
  stage: lint
  script:
    - cd base && molecule lint

lint:wm:
  <<: *gitlab_molecule
  stage: lint
  script:
    - cd wm && molecule lint

lint:desktop:
  <<: *gitlab_molecule
  stage: lint
  script:
    - cd desktop && molecule lint

lint:editors:
  <<: *gitlab_molecule
  stage: lint
  script:
    - cd editors && molecule lint

lint:finish:
  <<: *gitlab_molecule
  stage: lint
  script:
    - cd finish && molecule lint

lint:cloud:
  <<: *gitlab_molecule
  stage: lint
  script:
    - cd cloud && molecule lint

# ================================
# CONFIGURE

configure:debug:
  <<: *gitlab_molecule
  stage: configure
  script:
    - cd debug
    - molecule --debug destroy
    - molecule dependency
    - molecule create
    - molecule prepare

configure:setup:
  <<: *gitlab_molecule
  stage: configure
  script:
    - cd setup
    - molecule destroy
    - molecule dependency
    - molecule create
    - molecule prepare

configure:base:
  <<: *gitlab_molecule
  stage: configure
  script:
    - cd base
    - molecule destroy
    - molecule dependency
    - molecule create
    - molecule prepare

configure:wm:
  <<: *gitlab_molecule
  stage: configure
  script:
    - cd wm
    - molecule destroy
    - molecule dependency
    - molecule create
    - molecule prepare

configure:desktop:
  <<: *gitlab_molecule
  stage: configure
  script:
    - cd desktop
    - molecule destroy
    - molecule dependency
    - molecule create
    - molecule prepare

configure:editors:
  <<: *gitlab_molecule
  stage: configure
  script:
    - cd editors
    - molecule destroy
    - molecule dependency
    - molecule create
    - molecule prepare

configure:finish:
  <<: *gitlab_molecule
  stage: configure
  script:
    - cd finish
    - molecule destroy
    - molecule dependency
    - molecule create
    - molecule prepare

configure:cloud:
  <<: *gitlab_molecule
  stage: configure
  script:
    - cd cloud
    - molecule destroy
    - molecule dependency
    - molecule create
    - molecule prepare

# ================================
# TEST

test:debug:
  <<: *gitlab_molecule
  stage: test
  script:
    - cd debug
    - molecule converge
    - molecule idempotence
    - molecule side_effect
    - molecule verify
    - molecule destroy

test:setup:
  <<: *gitlab_molecule
  stage: test
  script:
    - cd setup
    - molecule converge
    - molecule idempotence
    - molecule side_effect
    - molecule verify
    - molecule destroy

test:base:
  <<: *gitlab_molecule
  stage: test
  script:
    - cd base
    - molecule converge
    - molecule idempotence
    - molecule side_effect
    - molecule verify
    - molecule destroy

test:wm:
  <<: *gitlab_molecule
  stage: test
  script:
    - cd wm
    - molecule converge
    - molecule idempotence
    - molecule side_effect
    - molecule verify
    - molecule destroy

test:desktop:
  <<: *gitlab_molecule
  stage: test
  script:
    - cd desktop
    - molecule converge
    - molecule idempotence
    - molecule side_effect
    - molecule verify
    - molecule destroy

test:editors:
  <<: *gitlab_molecule
  stage: test
  script:
    - cd editors
    - molecule converge
    - molecule idempotence
    - molecule side_effect
    - molecule verify
    - molecule destroy

test:finish:
  <<: *gitlab_molecule
  stage: test
  script:
    - cd finish
    - molecule converge
    - molecule idempotence
    - molecule side_effect
    - molecule verify
    - molecule destroy

test:cloud:
  <<: *gitlab_molecule
  stage: test
  script:
    - cd cloud
    - molecule converge
    - molecule idempotence
    - molecule side_effect
    - molecule verify
    - molecule destroy

